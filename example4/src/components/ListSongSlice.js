import { createSlice } from "@reduxjs/toolkit";


const ListSongSlice = createSlice({
    name: 'AddToMyList',
    initialState: [],
    
    reducers: {
        addSong: state => {
            
        }
    }
})